import { borders, radii } from './borders.base.token';
import { breakpoints } from './breakpoints.base.token';
import { colors } from './colors.base.tokens';
import { fonts, fontSizes, fontWeights, lineHeights, letterSpacings } from './fonts.base.tokens';
import { shadows } from './shadows.base.tokens';
import { icons, heights, sizes, maxWidths } from './sizes.base.tokens';
import { paddings, margins, space } from './spaces.base.tokens';
import { transitions } from './transition.base.token';

export default {
  rtl: true,
  borders,
  radii,
  breakpoints,
  colors,
  fonts,
  fontSizes,
  fontWeights,
  lineHeights,
  letterSpacings,
  shadows,
  space,
  heights,
  icons,
  sizes,
  maxWidths,
  paddings,
  margins,
  transitions,
};
